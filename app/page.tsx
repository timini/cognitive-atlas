import world from '../public/data/world.json';
import capitals from '../public/data/capitals.json';
export default function Home() {
 const rings = world.features.flatMap(f => f.geometry.type === 'Polygon' ? [f.geometry.coordinates] : f.geometry.coordinates);
 return <main><header className="atlas-header"><div className="brand"><span>◉</span> Cognitive Atlas</div><span className="kicker">A geography of spatial judgments</span></header><section className="intro"><div><span className="kicker">Experiment 001 · Gemini</span><h1>How does AI see the world?</h1><p>Reconstructing the Earth implied by a model’s estimates of distance.</p></div><span>20 capitals · 190 pairs · 10 samples</span></section><section className="surface"><div className="map-top"><strong>Actual Earth</strong><span>Research pipeline in preparation</span></div><svg className="map-svg" viewBox="-190 -95 380 190" role="img" aria-label="Real geography of the 20 selected national capitals"><g fill="#21354a" stroke="#526277" strokeWidth=".2">{rings.map((poly,i)=><path key={i} d={(poly as number[][][]).map(r=>'M'+r.map(p=>p[0]+','+(-p[1])).join('L')+'Z').join('')}/>)}</g>{capitals.map(p=><g key={p.id}><circle cx={p.longitude} cy={-p.latitude} r="1.2" fill="#aaf078"/><title>{p.capital_name}</title></g>)}</svg></section><p className="footnote">Natural Earth geography. Model results will appear only after real API responses have been collected and validated.</p></main>
}
